"""Fitness shaping.

    test_shaping_is_permutation_equivariant  centered ranks depend only on the ordering
    test_group_relative_shaping              GRPO-style variant, against a hand-worked case

Note for Phase 2: the global sort is a barrier. That cost is measured in E10, not asserted
here.
"""
